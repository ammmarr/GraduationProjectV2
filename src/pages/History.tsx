import PhraseTable from "@/components/custom/Table";
import ScreenWrapper from "@/Layouts/ScreenWrapper";

const History = () => {
  return (
    <ScreenWrapper>
      <PhraseTable />
    </ScreenWrapper>
  );
};

export default History;
